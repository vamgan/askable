name: static / quality

on:
  push:
    branches: [main]
    paths-ignore:
      - "site/**"
      - "README.md"
      - "examples/**"
      - ".github/workflows/pages.yml"
      - ".github/workflows/publish_release.yml"
      - "packages/**/CHANGELOG.md"
  pull_request:
    branches: [main]
    paths-ignore:
      - "site/**"
      - "README.md"
      - "examples/**"
      - ".github/workflows/pages.yml"
      - ".github/workflows/publish_release.yml"
      - "packages/**/CHANGELOG.md"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check example Askable package versions
        run: node scripts/check-example-askable-versions.mjs

      - name: Build core
        run: npm run build -w packages/core

      - name: Build all packages
        run: npm run build --workspaces --if-present
